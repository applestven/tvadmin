# TV Admin Panel

基于 Nuxt 3 + Ant Design Vue + TailwindCSS 的后台管理系统。

## 功能特性

- 📊 **仪表板** - 实时展示下载/转译任务统计
- 📥 **下载管理** - 管理视频下载任务
- 🔄 **转译管理** - 管理语音转文字任务
- 📝 **日志管理** - 实时查看服务器日志

## 技术栈

- **框架**: Nuxt 3
- **UI 组件**: Ant Design Vue 4.x
- **样式**: TailwindCSS
- **图表**: ECharts
- **语言**: TypeScript

## 项目结构

```
admin-panel/
├── server/                 # Nuxt Server
│   ├── api/               # API 路由
│   │   ├── tv/           # TV API 转发
│   │   ├── dv/           # DV API 转发
│   │   └── health.ts     # 健康检查
│   └── utils/            # 服务端工具
│       ├── config.ts     # 配置
│       ├── proxy.ts      # 代理逻辑
│       └── network-checker.ts  # 网络检测
├── composables/           # Vue 组合函数
│   ├── useApi.ts         # 通用 API
│   ├── useTvApi.ts       # TV API
│   ├── useDvApi.ts       # DV API
│   └── useNetworkStatus.ts  # 网络状态
├── pages/                 # 页面
│   ├── index.vue         # 仪表板
│   ├── download/         # 下载管理
│   ├── transcode/        # 转译管理
│   └── logs/             # 日志管理
├── components/            # 组件
│   ├── layout/           # 布局组件
│   └── charts/           # 图表组件
├── types/                 # 类型定义
├── layouts/               # 布局
└── assets/               # 静态资源
```

## 安装

```bash
# 安装依赖
npm install

# 开发模式
npm run dev

# 构建生产版本
npm run build

# 预览生产版本
npm run preview
```

## 环境配置

项目会自动检测网络环境：

- **公网可用**: 使用 `http://43.139.236.50:8686` 作为 API 服务器
- **内网模式**: 使用内网地址 `http://192.168.191.168:6789` (TV) 和 `http://192.168.191.168:3456` (DV)

所有 API 请求都通过 Nuxt Server 转发，浏览器只访问 Nuxt 服务。

## API 代理

| 前端请求 | 代理目标 |
|---------|---------|
| `/api/tv/*` | TV 下载服务 |
| `/api/dv/*` | DV 转译服务 |
| `/api/health` | 健康检查 |

## 开发说明

### 添加新 API

1. 在 `types/` 目录添加类型定义
2. 在 `composables/` 目录添加 API 封装
3. 在页面中使用组合函数

### 添加新页面

1. 在 `pages/` 目录创建 `.vue` 文件
2. Nuxt 会自动生成路由
3. 如需侧边栏显示，更新 `layouts/default.vue` 菜单配置

## License

MIT
